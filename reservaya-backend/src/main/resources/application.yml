spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/reservaya
    username: reservaya
    password: reservaya
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  flyway:
    enabled: false

app:
  cors:
    allowed-origins: "http://localhost:5173,https://app.reservaya.com"
